<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minesec.service.mapper.CustomerInfoMapper">
<!--    <select id="queryUserList" resultType="user">-->
<!--        select * from user-->
<!--    </select>-->

<!--    <select id="queryUserByID" resultType="user">-->
<!--        select * from user where id= #{id}-->
<!--    </select>-->

    <insert id="insertCustomerInfo" parameterType="com.minesec.service.dao.CustomerInfoDao">
        insert into t_customer_info(customer_id,create_time,update_time)
        values (#{customerId}, now(),now())
    </insert>
</mapper>
